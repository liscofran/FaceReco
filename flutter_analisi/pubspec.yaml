name: analisi_flutter
description: Demonstrates how to use the rtmp_broadcaster plugin.

publish_to: 'none'

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  camera: ^0.10.5+5
  web_socket_channel: ^2.1.0
  image: ^3.0.1
  typed_data: ^1.3.0
  async: ^2.11.0
  provider: ^6.0.1
  shared_preferences: ^2.0.13
  

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
